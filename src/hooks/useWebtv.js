/**
 * Hook WebTV : chaînes, programme du jour, sync heure serveur, playback URL, ended/loop.
 * Plan : REFACTORING-APP.md — à terme déplacer ici la logique webtv actuellement dans App.jsx (~450 lignes).
 * Pour l’instant la logique reste dans App.jsx ; ce hook peut être branché progressivement.
 */
export function useWebtv(language, page) {
  return {};
}
