/**
 * Hook Radio : état radio, lecture, seek serveur, MediaSession.
 * Plan : REFACTORING-APP.md — à terme déplacer ici toute la logique radio actuellement dans App.jsx (~650 lignes).
 * Pour l’instant la logique reste dans App.jsx ; ce hook peut être branché progressivement.
 */
export function useRadio(language, isAnyVideoPlaying) {
  return {};
}
