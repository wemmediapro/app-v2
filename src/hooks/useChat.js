/**
 * Hook Chat : Socket.io, conversations, messages, typing.
 * Plan : REFACTORING-APP.md — optionnel, si la page Messages est activée (~400 lignes).
 * Pour l’instant la logique reste dans App.jsx ; ce hook peut être branché progressivement.
 */
export function useChat() {
  return {};
}
