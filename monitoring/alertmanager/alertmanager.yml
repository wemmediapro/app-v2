global:
  resolve_timeout: 5m

route:
  receiver: default
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    # Ajouter webhook_configs (Slack, PagerDuty, etc.) pour notifier les alertes.
