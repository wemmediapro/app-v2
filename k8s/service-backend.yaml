apiVersion: v1
kind: Service
metadata:
  name: gnv-backend
  namespace: gnv-onboard
  labels:
    app.kubernetes.io/name: gnv-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gnv-backend
  ports:
    - name: http
      port: 3000
      targetPort: http
