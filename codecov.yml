# https://docs.codecov.com/docs/codecov-yaml
# Optionnel : nécessite le secret CODECOV_TOKEN sur GitHub pour les dépôts privés.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment: false
